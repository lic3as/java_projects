/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package operadoresaritmeticos;

/**
 *
 * @author alice
 */
public class OperadoresAritmeticos {
    
    public static void main(String[] args) {
        
        int n1 = 3;
        int n2 = 5;
        float m = (n1 + n2)/2; //media n1 e n2
        System.out.println("A média é igual a " + m);
        //o computador respeita a ordem de precedência, por isso, ma média, é necessário o uso de parênteses
        //utilizando a CLASSE MATH, eu posso:
        //  1. pegar o valor pi (Math.PI)
        //  2. calcular potência com a função pow (Math.pow(base, expoente))
        //  3. calcular a raiz quadrada com a função sqrt (Math.sqrt(valor))
        //  4. calcular a raiz cúbica com a função cbrt (Math.cbrt(valor))
        //  5. calcular o valor absoluto de um número com a função abs (Math.abs(-valor))
        //  6. arredondar um valor pra baixo utilizando a função floor (Math.floor(valor))
        //  7. arredondar um valor pra cima utilizando a função ceil (Math.ceil(valor))
        //  8. arredondar normalamente utilizando a função round (Math.round(valor))
        //  9. gerar um número aleatório entre 0.0 e 1.0 utilizando a função random (Math.random()) -> tem cmom usar uns macetes pra conseguir outro intervalo de números
        
    }
    
}
